Here is the procedure to create an executable from the Python
source code to run it everywhere.

1) Install py2exe (pip install py2exe_py2)
2) build.bat
3) zip the "ufcheck-X.X" directory and ship it

